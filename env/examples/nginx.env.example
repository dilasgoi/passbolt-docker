# NGINX environment configuration
# This file defines settings for the Nginx web server

# Suppress Nginx startup messages for cleaner logs
# Set to 1 to enable quiet logs, 0 to see all startup messages
NGINX_ENTRYPOINT_QUIET_LOGS=1

# Disable environment variable substitution in templates
# This prevents Nginx from trying to substitute env vars in config files
NGINX_ENVSUBST_TEMPLATE_DIR=/dev/null

# Optional: Client max body size
# Uncomment and adjust if you need to upload larger files
# NGINX_CLIENT_MAX_BODY_SIZE=10M

# Optional: SSL protocols to use
# Uncomment to specify allowed SSL/TLS protocols
# NGINX_SSL_PROTOCOLS="TLSv1.2 TLSv1.3"

# Optional: SSL ciphers
# Uncomment to specify allowed cipher suites
# NGINX_SSL_CIPHERS="HIGH:!aNULL:!MD5"

# Optional: Enable gzip compression
# Uncomment to enable gzip for text-based content
# NGINX_GZIP_ENABLED=1
